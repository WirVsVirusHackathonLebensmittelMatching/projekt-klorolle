<mvc:View 
	controllerName="com.wir.vs.virus.timeslots.Consumer.controller.FastLaneTypeSelect"
	xmlns:core="sap.ui.core" 
	xmlns:l="sap.ui.layout" 
	xmlns:mvc="sap.ui.core.mvc" 
	displayBlock="true"
	xmlns="sap.m">
		<Page id="page" title="{i18n>nextFastLane}">
			<content>
				<VBox alignItems="Center">
					<Label text="{i18n>comment}" class="sapUiSmallMarginTop" />
					<TextArea id="comment" width="500px" rows="10" />
					<Label text="{i18n>askAboutFastLaneType}" class="sapUiSmallMarginTop" />
					<Button id="shopping" text="{i18n>shopping}" press=".onProceedFastLane(false)" class="sapUiSmallMarginTop" />
					<Button id="pickup" text="{i18n>pickup}" press=".onProceedFastLane(true)" class="sapUiSmallMarginTop" />
				</VBox>
			</content>
		</Page>
</mvc:View>